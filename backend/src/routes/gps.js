const express = require('express');
const router = express.Router();

// TODO: Implement GPS tracking routes
// POST /track
// GET /history/:driverId
// GET /current/:routeId

module.exports = router;
